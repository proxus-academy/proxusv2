{
  "name": "@proxus/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build:backend": "node src/build/build-backend.mjs",
    "infra": "node src/cli.mjs"
  },
  "dependencies": {
    "@pulumi/gcp": "9.34.1",
    "@pulumi/pulumi": "3.257.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "esbuild": "0.28.2",
    "typescript": "^5.8.3",
    "vitest": "3.2.7"
  }
}
